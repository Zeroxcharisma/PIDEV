{% extends 'base.html.twig' %}

{% block title %}Utilisateur{% endblock %}

{% block body %}
{% block content %}

    <h1>Utilisateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdUtilisateur</th>
                <td>{{ utilisateur.idUtilisateur }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ utilisateur.username }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ utilisateur.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ utilisateur.prenom }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ utilisateur.password }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ utilisateur.tel }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ utilisateur.adresse }}</td>
            </tr>
            <tr>
                <th>IdRole</th>
                <td>{{ utilisateur.idRole }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ utilisateur.etat }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ utilisateur.email }}</td>
            </tr>
        </tbody>
    </table>

    <a  class="btn btn-outline-primary mr-1 mb-1 waves-effect waves-light" href="{{ path('utilisateur_index') }}">Retour</a>

    <a class="btn btn-outline-warning mr-1 mb-1 waves-effect waves-light"  href="{{ path('utilisateur_edit', {'idUtilisateur': utilisateur.idUtilisateur}) }}">Modifier</a>

    {{ include('utilisateur/_delete_form.html.twig') }}
{% endblock %}
{% endblock %}
