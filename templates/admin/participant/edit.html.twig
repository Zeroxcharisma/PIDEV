{% extends 'admin/baseBack.html.twig' %}

{% block title %}Edit Evenement{% endblock %}

{% block body %}
    <h1>Edit Participant</h1>

    {{ include('admin/participant/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('participant_index') }}">back to list</a>

    {{ include('/admin/participant/_delete_form.html.twig') }}
{% endblock %}
