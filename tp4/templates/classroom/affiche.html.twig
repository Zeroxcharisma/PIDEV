<table border="2">
    <tr>
        <td>id</td>
        <td>name</td>
        <td>Delete </td>

    </tr>
    
        {% for c in classroom %}
    <tr>
        <td>{{ c.id }}</td>
        <td>{{ c.name }}</td>
        <td>
        <a href="{{path('del',{'id':c.id}) }}">supprimer</a>
        </td>
    </tr>
    {% endfor %}




</table>